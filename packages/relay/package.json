{
  "name": "@pdatahub/relay",
  "version": "0.1.0",
  "description": "Cloudflare Worker relay for Hub-to-laptop WebSocket routing",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'lint TBD'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist .wrangler coverage"
  },
  "keywords": [
    "pdatahub",
    "cloudflare-workers",
    "durable-objects",
    "websocket"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.78.0"
  }
}
