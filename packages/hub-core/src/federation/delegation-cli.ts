/**
 * Phase 4 placeholder — delegation CLI subcommands.
 *
 * The Phase 4 subcommands (`pdatahub-hub delegate`, `pdatahub-hub
 * accept-delegation`, `pdatahub-hub delegate list`, `pdatahub-hub
 * delegation list`, `pdatahub-hub delegation revoke`) all live here. Phase 2a
 * exports just the public placeholder type so `src/index.ts` can already
 * import `delegation-cli` without a circular dependency appearing later.
 *
 * The type is intentionally empty — `pdatahub-hub` will pull in the real
 * implementation when this file is filled in.
 */

export type DelegationCliSubcommand = never;
